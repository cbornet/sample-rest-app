{
  "openapi": "3.0.1",
  "paths": {
    "/api/orders/[( ${order.id} )]": {
      "put": {
        "summary": "Update order [( ${order.id} )]",
        "description": "Update order [( ${order.id} )]. It's not possible to modify the product name",
        "requestBody": {
          "content": {
            "application/json":{
              "schema":{
                "$ref":"#/components/schemas/Order"
              }
            }
          }
        }
      }[# th:if="${order.id} > 100"],
      "delete": { "summary": "Delete order [( ${order.id} )]" }
      [/]
    },
[# th:if="${order.customer}"]
    "/api/customers/[( ${order.customer.id} )]": {
      "get": { "summary": "Get this order's customer (id=[( ${order.customer.id} )])" }
    },
[/]
    "/api/orders": {
      "get": { "summary": "Get all orders" }
    }
  },
  "components": {
    "schemas": {
      "Order": {
        "type": "object",
        "properties": {
          "cost": {
            "type": "number",
            "example": [( ${order.cost} )]
          },
          "customer": {
            "type": "object",
            "properties": {
              "id": {
                "type": "integer",
                "example": [( ${order.customer.id} )]
              }
            }
          }
        }
      }
    },
    "securitySchemes": {
      "jwt": {
        "type": "http",
        "description": "JWT Authentication",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      },
      "basic": {
        "type": "http",
        "description": "Basic Authentication",
        "scheme": "basic"
      }
    }
  },
  "security": [
    {
      "jwt": [

      ]
    },
    {
      "basic": [

      ]
    }
  ]
}
